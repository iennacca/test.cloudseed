namespace CloudSeedApp

// open Configuration

module ServiceTree = 

    type Settings = {
        AppConfiguration: Configuration.AppConfiguration
    }
    type Workflows = {
        NotAWorkflow: int
    }
    type WorflowIOs = {
        NotAnIO: int
    }